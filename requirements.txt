# Core dependencies
pandas>=1.3.0
lxml>=4.6.0
openpyxl>=3.0.7
python-dateutil>=2.8.0

# Schema validation (Pydantic v2)
pydantic>=2.0.0

# PDF processing
pdfplumber>=0.9.0

# REST API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# LIDC-IDRI dataset integration (optional)
# pylidc>=0.2.0  # Uncomment to enable PYLIDC integration

# Note: tkinter is usually included with Python
# If missing, install with: apt-get install python3-tk (Linux)
